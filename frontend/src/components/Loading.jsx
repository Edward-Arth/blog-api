const Loading = () => {
    return (
        <div>
            <div className="loadingIndic">Loading</div>
            <div className="loadingExplanation">My API is slow to start because the host platform spins down with inactivity. It will be much faster after this!</div>
        </div>
    )
}

export default Loading;